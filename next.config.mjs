/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true, // Ensures public images load correctly
  },
  reactStrictMode: true,
};

export default nextConfig



